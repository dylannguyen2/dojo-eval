[project]
name = "dojo-figma-eval"
version = "1.0.0"
description = "Figma task verification server and reward functions"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn>=0.27.0",
    "requests>=2.31.0",
    "pydantic>=2.5.0",
]

[tool.uv]
dev-dependencies = []
